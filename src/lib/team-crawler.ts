export default class TeamCrawler {
    constructor () {}

    public static crawlTeamData(gameweek?: number) {
        // will crawl team data
    }

    public static saveTeamData() {
        // will save team data to firebase
    }
}
