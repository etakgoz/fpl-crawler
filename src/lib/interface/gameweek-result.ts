interface IGameweekResult {
    playerId: string,
    gameweekId: number,
    points: number,
    transferCost: number,
    nTransfers: number,
    activeChip: string
}

export default IGameweekResult;
